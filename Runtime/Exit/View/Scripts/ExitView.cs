using Exit.View.Components;
using UnityEngine;

namespace Exit.View
{
    public class ExitView : MonoBehaviour
    {
        [field: SerializeField] internal ExitPopup Popup { get; private set; }
    }
}